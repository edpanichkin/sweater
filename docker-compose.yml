version: "3.7" 
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: 123456
      ports:
      - 5432:5432

  adminer:
    image: adminer
    restart: always
    ports:
      - 6080:8080
  java:
    build: .
